@using Application.Boundaries.UseCases.Scheduling.Dto.Input
@using Application.UseCases.Scheduling.Dto.Input

<EditTreatmentSession Model="Model" PatientInfo="PatientInfo" />

@code {
    [Parameter]
    public Guid PatientReferenceId { get; set; }

    [Parameter]
    public DateTime Start { get; set; }

    [Parameter]
    public DateTime End { get; set; }

    public ITreatmentSessionInput Model { get; set; }

    public PatientViewModel PatientInfo { get; set; }

    protected override void OnInitialized()
    {
        Model = new TreatmentSessionInput();
        PatientInfo = new PatientViewModel();

        Model.PatientReferenceId = PatientReferenceId;
        Model.Start = Start;
        Model.Start = End;
        Model.Status = "Requested";
    }
}